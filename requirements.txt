# Core runtime dependencies for hwval.
#
# IMPORTANT: this file is what Streamlit Community Cloud installs for the
# demo deployment, so it MUST be self-sufficient on its own (no `-e .`, no
# extras file) and MUST NOT include tensorflow/tensorflow-cpu -- the CPU
# wheel is several hundred MB and blows well past Streamlit Cloud's free-tier
# build size/time budget. hwval.ml.train_tf / predict already degrade to a
# PCA-based autoencoder fallback when TensorFlow is not importable, so the
# app keeps working, just without the LSTM autoencoder score.
#
# Mirrors the `dependencies` list in pyproject.toml -- keep the two in sync
# by hand when either changes.
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
joblib>=1.3.0
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.2.0
jinja2>=3.1.3
tabulate>=0.9.0
langchain>=1.0.0
langchain-core>=1.0.0
langgraph>=1.0.0
mcp>=1.2.0,<2.0.0
streamlit>=1.36.0
